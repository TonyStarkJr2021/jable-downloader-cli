# Jable + MissAV Downloader v2.1.1

修复 Web 任务日志在下载过程中长时间不更新、完成时才集中显示的问题。

## 修复

- Linux Web 服务使用伪终端启动下载命令，让 N_m3u8DL-RE 持续输出终端进度
- 同时识别普通换行和用于原地刷新的回车进度
- 当前进度独立更新，避免大量重复进度行挤掉重要日志
- 前端继续自动滚动到最新内容
- 清理 ANSI 颜色和光标控制字符，保持日志易读
- 保留 M3U8 签名地址隐藏逻辑

本版本不修改下载源解析、成品路径、Web 账号、端口或现有媒体文件。
